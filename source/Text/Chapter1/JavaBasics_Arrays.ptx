<?xml version="1.0" encoding="UTF-8"?>

<chapter xml:id="ch-java-basics-arrays" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Java Basics - Arrays</title>

  <introduction>
    <p> Text before the first section. </p>
  </introduction>
   
  <objectives>
	<ul>
	  <li>Refresh on how to declare and initialize arrays. </li>
	  <li>Refresh on how to edit elements of arrays.</li>
	</ul>
  </objectives>

  <section xml:id="JB-arrays-starter">
		<title>Starter Level</title>
		
		<!--Create an array of 37 floats and store the number 3.14 in the last slot/bin of the array.-->
		
	</section>

	<section xml:id="JB-arrays-growth">
		<title>Growth Level</title>
		<!--Given an array of floating point numbers, named data, write a loop that calculates 
			the sum of every second number in the array.  The sum should include the first, third, etc. numbers in the array.-->

		<p>
		</p>
		
	</section>

	<section xml:id="JB-arrays-stretch">
		<title>Stretch Level</title>
		<!--Create a function that accepts a partially-filled array of Strings and prints those Strings out to the console. 
		
			void printStrings(String[] stringArray, int numStrings) {
		   for (int i = 0; i < numStrings; i++) {
			      println(stringArray[i]);
		   }
}

Given a partially-filled array of Strings, named foods, fill in the code below so that it counts the number of entries 
in the foods array that are not equal to "pizza".  Print the result to the console.  Assume that the integers MAX_SIZE 
(the maximum number of entries in the array) and numFoods (the number of current entries in the array) have been 
declared and assigned valid values.

int count = _____0_____;
for( int i=0; _____i < numFoods________; ______i++______)
    if (__________!foods[i].equals("pizza")_________)
        count++;
println(count);

-->

		<p>
		</p>
		
	</section>


</chapter>